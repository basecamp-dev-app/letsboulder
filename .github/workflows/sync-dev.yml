name: Sync Dev

on:
  workflow_dispatch:

jobs:
  merge-to-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Merge main to dev
        run: git push origin main:dev
