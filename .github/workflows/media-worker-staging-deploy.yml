name: Media Worker Deploy (Staging)

on:
  push:
    branches:
      - staging
    paths:
      - "apps/media-worker/**"
      - ".github/workflows/media-worker-staging-deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: media-worker-staging
  cancel-in-progress: false

jobs:
  deploy-staging:
    name: Deploy Media Worker (Staging)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: Staging
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.23.2"
          cache: npm
          cache-dependency-path: apps/media-worker/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: apps/media-worker

      - name: Typecheck media worker
        run: npm run check
        working-directory: apps/media-worker

      - name: Validate staging media worker secrets
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          INGRESS_SECRET: ${{ secrets.INGRESS_SECRET }}
          INTERNAL_ORIGIN_SECRET: ${{ secrets.INTERNAL_ORIGIN_SECRET }}
        run: |
          set -euo pipefail
          required=(SUPABASE_URL SUPABASE_ANON_KEY SUPABASE_SERVICE_ROLE_KEY INGRESS_SECRET INTERNAL_ORIGIN_SECRET)
          for name in "${required[@]}"; do
            if [ -z "${!name:-}" ]; then
              echo "::error::$name is required in the Staging GitHub environment."
              exit 1
            fi
          done

      - name: Ensure staging media infrastructure exists
        working-directory: apps/media-worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail

          if npx --no-install wrangler r2 bucket list | grep -Fq 'lb-staging-media-private'; then
            echo 'lb-staging-media-private already exists'
          else
            npx --no-install wrangler r2 bucket create lb-staging-media-private
          fi

          if npx --no-install wrangler r2 bucket list | grep -Fq 'lb-staging-media-public'; then
            echo 'lb-staging-media-public already exists'
          else
            npx --no-install wrangler r2 bucket create lb-staging-media-public
          fi

          if npx --no-install wrangler queues list | grep -Fq 'media-transform-queue-staging'; then
            echo 'media-transform-queue-staging already exists'
          else
            npx --no-install wrangler queues create media-transform-queue-staging
          fi

      # The legacy staging Worker has SUPABASE_URL stored as a plain-text var.
      # Deploying the current config first removes that old binding so the same
      # name can then be recreated as a Worker secret without Cloudflare 10053.
      - name: Reconcile staging worker bindings
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/media-worker
          command: deploy --env staging

      - name: Upload staging media worker secrets
        working-directory: apps/media-worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          INGRESS_SECRET: ${{ secrets.INGRESS_SECRET }}
          INTERNAL_ORIGIN_SECRET: ${{ secrets.INTERNAL_ORIGIN_SECRET }}
        run: |
          set -euo pipefail
          for name in SUPABASE_URL SUPABASE_ANON_KEY SUPABASE_SERVICE_ROLE_KEY INGRESS_SECRET INTERNAL_ORIGIN_SECRET; do
            printf '%s' "${!name}" | npx --no-install wrangler secret put "$name" --env staging
          done

      - name: Deploy staging media worker
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/media-worker
          command: deploy --env staging

      - name: Smoke test staging media endpoint
        run: |
          set -euo pipefail

          status="$(
            curl \
              --silent \
              --show-error \
              --retry 12 \
              --retry-all-errors \
              --retry-delay 5 \
              --output /dev/null \
              --write-out '%{http_code}' \
              --request POST \
              https://static.staging.letsboulder.com/enqueue
          )"

          if [ "$status" != "401" ]; then
            echo "::error::Expected 401 from staging media worker, got $status"
            exit 1
          fi

          echo "Staging media worker is reachable and ingress auth is enforced."
