name: Supabase Migrations

on:
  workflow_run:
    workflows:
      - CI
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: supabase-migrations-${{ github.ref }}
  cancel-in-progress: false

jobs:
  migrate-prod:
    name: Apply Supabase migrations (Production)
    if: |
      github.event_name == 'workflow_dispatch' ||
      (
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.head_branch == 'main'
      )
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          ref: ${{ github.event_name == 'workflow_dispatch' && github.ref || github.event.workflow_run.head_sha }}

      - name: Detect migration changes
        id: migration_changes
        shell: bash
        run: |
          MIGRATION_COUNT=$(ls supabase/migrations/*.sql 2>/dev/null | wc -l)
          echo "Migration count: $MIGRATION_COUNT"
          echo "count=$MIGRATION_COUNT" >> "$GITHUB_OUTPUT"

          THRESHOLD=100
          if [ "$MIGRATION_COUNT" -gt "$THRESHOLD" ]; then
            echo "migration_count_exceeded=true" >> "$GITHUB_OUTPUT"
          else
            echo "migration_count_exceeded=false" >> "$GITHUB_OUTPUT"
          fi

          if [ "${{ github.event_name }}" = 'workflow_dispatch' ]; then
            echo 'changed=true' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if git diff --name-only HEAD^ HEAD -- 'supabase/migrations/**' '.github/workflows/supabase-migrations.yml' | grep -q .; then
            echo 'changed=true' >> "$GITHUB_OUTPUT"
          else
            echo 'changed=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Migration count warning
        if: steps.migration_changes.outputs.migration_count_exceeded == 'true'
        run: |
          echo "::warning::Migration count (${{ steps.migration_changes.outputs.count }}) exceeds $THRESHOLD. Consider squashing migrations to reduce operational risk."

      - name: Setup Supabase CLI
        if: steps.migration_changes.outputs.changed == 'true'
        uses: supabase/setup-cli@v1
        with:
          version: 2.84.2

      - name: Link Supabase project
        if: steps.migration_changes.outputs.changed == 'true'
        run: supabase link --project-ref "$SUPABASE_PROJECT_REF"
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}

      - name: Apply migrations
        if: steps.migration_changes.outputs.changed == 'true'
        run: supabase db push --linked --include-all --yes
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}

      - name: Skip when no migration changes
        if: steps.migration_changes.outputs.changed != 'true'
        run: |
          echo 'No migration-related changes in the successful push workflow.'
          echo 'Skipping Supabase migration apply.'

  migrate-dev:
    name: Apply Supabase migrations (Development)
    if: |
      github.event_name == 'workflow_dispatch' ||
      (
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.head_branch == 'main'
      )
    runs-on: ubuntu-latest
    environment: Development
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          ref: ${{ github.event_name == 'workflow_dispatch' && github.ref || github.event.workflow_run.head_sha }}

      - name: Detect migration changes
        id: migration_changes
        shell: bash
        run: |
          MIGRATION_COUNT=$(ls supabase/migrations/*.sql 2>/dev/null | wc -l)
          echo "Migration count: $MIGRATION_COUNT"
          echo "count=$MIGRATION_COUNT" >> "$GITHUB_OUTPUT"

          THRESHOLD=100
          if [ "$MIGRATION_COUNT" -gt "$THRESHOLD" ]; then
            echo "migration_count_exceeded=true" >> "$GITHUB_OUTPUT"
          else
            echo "migration_count_exceeded=false" >> "$GITHUB_OUTPUT"
          fi

          if [ "${{ github.event_name }}" = 'workflow_dispatch' ]; then
            echo 'changed=true' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if git diff --name-only HEAD^ HEAD -- 'supabase/migrations/**' '.github/workflows/supabase-migrations.yml' | grep -q .; then
            echo 'changed=true' >> "$GITHUB_OUTPUT"
          else
            echo 'changed=false' >> "$GITHUB_OUTPUT"
          fi

      - name: Migration count warning
        if: steps.migration_changes.outputs.migration_count_exceeded == 'true'
        run: |
          echo "::warning::Migration count (${{ steps.migration_changes.outputs.count }}) exceeds $THRESHOLD. Consider squashing migrations to reduce operational risk."

      - name: Setup Supabase CLI
        if: steps.migration_changes.outputs.changed == 'true'
        uses: supabase/setup-cli@v1
        with:
          version: 2.84.2

      - name: Sync dev schema from prod
        if: steps.migration_changes.outputs.changed == 'true'
        run: |
          supabase link --project-ref "$PROD_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"
          supabase db pull --linked --schema public --skip-migration-history
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          PROD_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}

      - name: Link dev project and apply migrations
        if: steps.migration_changes.outputs.changed == 'true'
        run: |
          supabase link --project-ref "$SUPABASE_DEV_PROJECT_REF" --password "$SUPABASE_DEV_DB_PASSWORD"
          supabase db push --linked --include-all --yes
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DEV_PROJECT_REF: ${{ secrets.SUPABASE_DEV_PROJECT_REF }}
          SUPABASE_DEV_DB_PASSWORD: ${{ secrets.SUPABASE_DEV_DB_PASSWORD }}

      - name: Skip when no migration changes
        if: steps.migration_changes.outputs.changed != 'true'
        run: |
          echo 'No migration-related changes in the successful push workflow.'
          echo 'Skipping Supabase migration apply.'
